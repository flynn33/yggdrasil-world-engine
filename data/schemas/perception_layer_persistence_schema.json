{
  "status": "placeholder_awaiting_finalized_content",
  "system": "perception_layer_persistence_schema",
  "purpose": "Placeholder for perception layer persistence schema.",
  "inputs": [
    "perception engine outputs",
    "shared world truth constraints"
  ],
  "outputs": [
    "perception persistence schema contract"
  ],
  "invariants": [
    "all_meaningful_generation_must_be_ash_derived",
    "fixed_cosmology_must_remain_locked",
    "perception_must_not_rewrite_shared_world_truth",
    "forsetti_governs_activation_ywe_governs_truth"
  ],
  "ash_alignment_contract": {
    "authority": "YWE_ASP_CORE_MATH_REBUILD_PACKAGE",
    "math_basis": "F2^9 full-state ASH vector with exactly 16 canonical codewords and x' = x XOR c transitions",
    "required_inputs": [
      "CosmicPatternSnapshot",
      "DiagnosticEnvelope"
    ],
    "required_materialization": "GenerationPlan",
    "provenance_fields": [
      "cosmic_pattern_snapshot_ref",
      "diagnostic_ref",
      "generation_plan_ref",
      "source_ash_refs"
    ],
    "truth_boundary": "GenerationPlan may guide materialization; downstream engines and adapters must not author ASH truth or YWE domain truth.",
    "records": [
      "PerceptionStateRecord",
      "OverlayManifest",
      "VisibilityRules",
      "truth_substrate_ref",
      "perception_overlay_does_not_rewrite_shared_world_truth",
      "geography_rewrite"
    ]
  },
  "ash_required_fields": {
    "cosmic_pattern_snapshot_ref": "CosmicPatternSnapshot",
    "diagnostic_ref": "DiagnosticEnvelope",
    "generation_plan_ref": "GenerationPlan",
    "source_ash_refs": "SourceASHRefs"
  },
  "x-ywe-legacy-dependencies": [
    "core/perception_engine",
    "perception divergence rules"
  ],
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ywe.local/schemas/perception_layer_persistence_schema.json",
  "title": "Perception Layer Persistence Schema",
  "x-ywe-requirement-id": "YWE-M2-SCHEMA-DATA-SCHEMAS-PERCEPTION-LAYER-PERSISTENCE-SCHEMA-JSON",
  "type": "object",
  "required": [
    "system",
    "inputs",
    "outputs",
    "invariants",
    "ash_required_fields"
  ],
  "properties": {
    "system": {
      "type": "string",
      "minLength": 1
    },
    "inputs": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "outputs": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "invariants": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "ash_required_fields": {
      "type": "object",
      "properties": {
        "cosmic_pattern_snapshot_ref": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
        },
        "diagnostic_ref": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
        },
        "generation_plan_ref": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*$"
        },
        "source_ash_refs": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
