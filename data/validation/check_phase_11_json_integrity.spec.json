{
  "id": "check_phase_11_json_integrity",
  "purpose": "Parse all JSON files in data/schemas, data/validation, examples, manifests, and check_specs.",
  "json_paths": [
    "data/schemas/**/*.json",
    "data/validation/**/*.json",
    "examples/**/*.json"
  ]
}
