{
  "id": "check_no_static_only_location_model",
  "purpose": "Fail if new Phase 11 docs describe locations as static-only.",
  "forbidden_patterns": [
    "static-only location model",
    "locations cannot change",
    "location state is immutable"
  ]
}
