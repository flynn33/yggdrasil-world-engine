version: "0.1"
status: placeholder_awaiting_finalized_content
meta:
  system: ash_runtime_generation_flow
  purpose: Placeholder awaiting finalized content for ash_runtime_generation_flow.
  invariants:
    - all_meaningful_generation_must_be_ash_derived
    - fixed_cosmology_must_remain_locked
    - perception_must_not_rewrite_shared_world_truth
    - forsetti_governs_activation_ywe_governs_truth

ash_asp_core_math_rebuild_addendum:
  authority: YWE_ASP_CORE_MATH_REBUILD_PACKAGE
  math_basis: F2^9 full-state ASH vector with fixed 16 canonical codewords
  transition_semantics: "x' = x XOR c"
  required_packets:
    - CosmicPatternSnapshot
    - DiagnosticEnvelope
    - GenerationPlan
  provenance_fields:
    - cosmic_pattern_snapshot_ref
    - diagnostic_ref
    - generation_plan_ref
    - source_ash_refs
  boundary_rule: generation planning may guide materialization but must not author ASH truth or YWE domain truth

  flow_records:
    - CosmicPatternSnapshot
    - DiagnosticEnvelope
    - GenerationPlan
  materialization_rule: runtime flow stops at GenerationPlan before adapter materialization.
