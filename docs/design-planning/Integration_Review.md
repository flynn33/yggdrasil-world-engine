# Planning Directory — Integration Review

**Status:** Draft publication; not ready for merge or normative incorporation.
**Scope:** `docs/design-planning/` only.

## Completed for storage

The original archive manifests were verified. The retained value experiment was rerun successfully before publication. Candidate status and inherited evidence dates are retained. The new storage decision supersedes the earlier unverified external-folder instruction. No existing engine file, acceptance artifact, version, source pin, roadmap gate, or workflow is intentionally changed.

## Required before merge

The current Repository Artifact Classification Policy requires classification and scope metadata to change with additions and requires exact tracked-path snapshots. Those obligations apply to this directory too. The current catch-all rules must not accidentally promote experimental code, schemas, reports, or proposals into normative engine authority.

In the same final reviewed change, assign the new paths appropriate informative, example, or historical roles, and assign the current storage/continuity record its scoped governance role. Recompute the classification and scope snapshots from an actual complete checkout, review all resulting counts, and run the registered repository checks. Do not invent a path digest, reset quality debt, weaken a validator, or edit immutable M0/M1 evidence to make the change pass.

A complete checkout was unavailable during this publication. Consequently, no exact new repository path digest, full regression pass, schema-debt decrease, or classification acceptance is asserted. The review branch preserves the work while these integration obligations remain open; it is not merged.

## Required before adopting the value candidate

Review existing consumers and fixtures for exact `bits` encodings, membership metadata, record closedness, numeric parsing, reference/signature distinctions, and type-specific validation. Resolve affected method/realization prerequisites. Retain all unresolved normalization/recovery questions outside this slice. Adopt only source-backed decisions through the existing project authority.

## Evidence boundaries

The candidate verifier tests an isolated two-value model and experimental schema. It is not the official M3 oracle, the full engine suite, or an independent human acceptance review. Python and its two verification libraries are testing-only, not runtime dependencies.
