# M2 Packet Readiness — Earlier Review

The [readiness review](M2_Readiness_Review.md) and [packet contract draft](M2_Packet_Contract.prepared.md) preserve the prior bounded investigation. The subsequent two-value experiment is in `../ash-values/`.

## Evidence boundary

The recorded packet-schema result was `BLOCKED`; that is a diagnostic finding, not an unsuccessful new run in this planning-publication session. Its original reports and probe tools are retained with unchanged bytes. The original source snapshot is not copied into this directory because it is an existing YWE repository artifact, not a second specification to maintain.

`inspection/snapshot_verification.json` is historical metadata about the earlier recovery. Relative paths and old temporary locations inside historical machine reports identify that earlier environment. Source identity remains the Git blob `1d1891fce3138fd87ce69483b47f9b093094fe71` at commit `2db1230f638cd065d791c05f1adb7b4b51505c57`, repository path `data/schemas/ash_generation_packet_schema.json`.

For a new probe, supply that verified source from a checkout and a fresh output path. Do not assume the old `inspection` snapshot exists here. Read the probe's arguments before invocation and preserve earlier results. No readiness probe was rerun merely to relocate these files.

The superseded prepared records and original transport ZIP are not duplicated. Current planning continuity belongs to `../../Project_Record.md` and the named current handoff.
