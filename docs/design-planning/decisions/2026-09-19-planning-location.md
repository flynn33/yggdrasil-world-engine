# Planning Location Decision

**ID:** YWE-PLAN-LOC-20260919
**Date:** September 19, 2026
**Status:** Owner-approved storage and publication direction; executing through the connected owner account.
**Scope:** Yggdrasil World Engine design and planning artifacts only.

## Owner direction

> agreed, we will push this to GitHub as part of the designing and planning. We will keep them in a dedicated directory for designing and planning.

This approves repository storage and a scoped GitHub push of the design/planning work. The selected repository-relative location is `docs/design-planning/`, consistent with the existing documentation layout. It does not approve every design proposal contained in those files.

## Treatment

Maintain drafts, proposed object contracts, candidate schemas and fixtures, experimental verification tools, source-reference metadata, reports, and continuation records in this dedicated directory. Retain clear status labels and original evidence provenance.

Use `Project_Record.md` as the single intended current continuation record here, with dated handoffs pointing to it. The prior prepared records are superseded as working copies; any independently recovered pre-existing authority must be reconciled rather than overwritten. This does not change the milestone/status authority of `data/governance/specification_roadmap.json` or the authority of accepted focused contracts.

The earlier instructions to save every record in an assumed external workspace are superseded. No such external record location had been verified. Only issued coding-agent instruction packages must remain external.

## Not authorized by this decision

No automatic approval of candidate wire formats, source or method upgrade, platform work, dependency import, release/tag, force-push, unrelated source rewrite, or weakening of validation. This publication must not silently enroll experimental content as an approved specification.

## Additional owner authorization

> You may push these to the repository as me, and use my Admin bypass, no ai attribution.

This supplies explicit owner-account commit/push authority and permission to use an existing admin bypass if necessary. It does not authorize disabling protections, force-pushing over unrelated work, or approving candidate engine semantics. The authenticated account is `flynn33`; the repository response confirms admin/write access. Verify the resulting author and committer and omit attribution trailers.

## Execution disposition

Use `design-planning/m2-foundations` for this durable planning publication while preserving main at the existing baseline. The complete planning work remains under `docs/design-planning/`. The full-repository metadata and regression obligations still gate main integration; they are not waived by the authorization to preserve this work on a branch.

Git blob/tree creation and branch creation have succeeded. Final commit/ref and read-back results belong in the current Project Record and publication receipt. Earlier claims that the connection was read-only are historical and superseded by observed write capability. No repository settings or workflow changes are authorized or required by this transaction.
